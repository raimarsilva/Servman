#include "listing.h"
#include "window.h"

int main(int argc, char *argv[]){
    //system("systemctl status rabbitmq-server.service");
    //listServices();
    init_gui(argc, argv);
    //printf("%s\n", services);
    return 0;
}